## 【C言語 Kiloエディタ写経】カーソルが動かない・左上に固定される時のチェックポイント

### 発生したエラー事象：テスト入力（w,a,s,d）が反映されない

Kiloチュートリアルの第3章（Step 45付近）において、一時的な動作テストとして実装したはずの `w`, `a`, `s`, `d` キーでのカーソル移動が機能しない問題が発生しました。矢印キーの動作確認はできたものの、カーソルが常に画面の左上に固定されてしまうような挙動となります。

### 直接的な原因：エスケープシーケンスの消し忘れ

原因は、`editorRefreshScreen()` 関数の中に、カーソルを画面左上に強制的に戻すエスケープシーケンス `\x1b[H` が残ってしまっていたことでした。

* **誤った状態:** 動的にカーソル位置を指定した直後に、`abAppend(&ab, "\x1b[H", 3);` が実行され、描画のたびにカーソルが左上に引き戻されていた。
* **正しい状態:** この1行を削除することで、管理している座標（`E.cx`, `E.cy`）通りにカーソルが正しく描画される。

### 根本的な原因：チュートリアルの「サイレント削除」の罠

なぜこの1行を消し忘れてしまったのか、その根本的な理由はチュートリアルサイトの**変更箇所のハイライト（UI）**にありました。

* **Step 40の表記:** 削除される行には、明確に**「打ち消し線」**が引かれていた。
* **Step 44の表記:** 該当のエスケープシーケンスを削除するステップだったが、打ち消し線は表示されず、**該当行がしれっと消えているだけ**だった。

**【教訓】**
追加部分のハイライトにばかり視線が誘導されると、「既存コードの削除」を見落としてしまいます。チュートリアルの装飾を鵜呑みにせず、変更前後の「差分（Diff）」全体をフラットな目線で確認することが重要です。

### おまけ：エスケープシーケンスのタイポに注意

もう一つの躓きやすいポイントとして、`editorReadKey()` 関数内のキー入力判定処理があります。
HomeキーやEndキーに対応するエスケープシーケンスを判定する際、`} else if (seq[0] == 'O') {` と記述すべき箇所で、大文字の「オー（`O`）」を数字の「ゼロ（`0`）」と見間違えてタイポしやすいので注意が必要です。
