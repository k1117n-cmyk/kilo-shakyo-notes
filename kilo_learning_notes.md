# Build Your Own Text Editor 写経メモ

## このノートについて

このノートは、snaptoken 氏の [Build Your Own Text Editor](https://viewsourcecode.org/snaptoken/kilo/index.html) を写経しながら、`kilo.c` の構造や詰まりやすい点を整理した学習メモです。

元記事の翻訳や転載ではありません。元記事を読み、手元でコードを書き、コンパイルし、動かしながら理解したことを補足としてまとめています。

Kilo は antirez 氏による小さな C 製テキストエディタで、snaptoken 氏のチュートリアルでは、それをもとにしたエディタを 1 step ずつ作っていきます。端末制御、Raw mode、画面描画、ファイル I/O、検索、シンタックスハイライトまでを、外部ライブラリなしの 1 ファイル C プログラムとして学べる教材です。

このノートの目的は、今後同じように写経する人が、以下のような場面で迷いにくくなることです。

- 今のエラーが写経ミスなのか、まだ途中 step だからなのか判断したい
- 差分で削除された行を見落としていないか確認したい
- C の警告の意味を調べるきっかけが欲しい
- 完成した `kilo.c` の大まかな構造を見直したい

## 元記事の読み方

元記事は、完成コードを最初から順に解説する形式ではなく、差分を積み上げてエディタを作っていく形式です。

各 step では、数行の追加、変更、削除を行います。多くの step はコンパイルしてすぐに動作確認できますが、見た目には変化がない step や、コードの意味があとから説明されるように感じる step もあります。

そのため、写経中は次の点を意識すると進めやすいです。

- 追加された行だけでなく、削除された行も確認する
- その step のコードがコンパイル可能な状態か確認する
- 警告とエラーを分けて読む
- 動作がおかしい場合は、元記事の該当 step のコードと自分のコードを diff する
- 「まだ作りかけだから起きている挙動」と「単純な写経ミス」を切り分ける

元記事の Appendix でも、詰まったときはチュートリアル側の step ごとの `kilo.c` と自分のコードを比較する方法が紹介されています。

## コンパイル環境

このリポジトリの `makefile` では、次のようなオプションでコンパイルしています。

```makefile
$(CC) kilo.c -o kilo -Wall -Wextra -pedantic -std=c99
```

それぞれの意味はおおよそ次の通りです。

- `-Wall`: 代表的な警告を有効にする
- `-Wextra`: 追加の警告を有効にする
- `-pedantic`: 標準 C から外れた書き方に厳しくする
- `-std=c99`: C99 としてコンパイルする

写経中は「コンパイルできるか」だけでなく、「警告が出ていないか」も見ると学習になります。警告は、すぐに実行時エラーにならなくても、C として曖昧だったり、環境差が出やすかったりする箇所を教えてくれます。

## `(void)` の省略による警告

C では、引数なしの関数を書くときに注意が必要です。

```c
void initEditor() {
  ...
}
```

このような書き方は、C では「引数を取らない関数」と厳密に同じ意味にはなりません。引数がないことを明示するなら、次のように書きます。

```c
void initEditor(void) {
  ...
}
```

同じように、`main()` も引数を使わないなら次のように書けます。

```c
int main(void) {
  return 0;
}
```

ただし、このリポジトリの完成版 `kilo.c` ではファイル名をコマンドライン引数で受け取るため、`main` は次の形です。

```c
int main(int argc, char *argv[]) {
  ...
}
```

チュートリアルの途中 step では、説明を簡単にするために `main()` や `disableRawMode()` のような形で書かれている箇所があります。環境やコンパイラの設定によっては、これが警告として出ることがあります。

この警告は、元記事が悪いというより、C の関数宣言の仕様を知るよいきっかけとして扱うのがよいです。

## `kilo.c` の全体像

完成版の `kilo.c` は、大きく見ると次の部品でできています。

- 端末制御
- 入力処理
- エディタ全体の状態管理
- 行データの管理
- 画面描画
- ファイル読み書き
- 検索
- シンタックスハイライト

実行時の大まかな流れは次の通りです。

```text
main()
  -> enableRawMode()
  -> initEditor()
  -> 必要なら editorOpen()
  -> while (1)
       -> editorRefreshScreen()
       -> editorProcessKeypress()
```

Kilo は、キー入力を 1 つ読み、内部状態を更新し、画面全体を再描画する、という流れを繰り返します。

## エディタ全体の状態

中心になるのは、グローバル変数 `E` です。

```c
struct editorConfig E;
```

`E` には、エディタの状態がまとめて入っています。

- `cx`, `cy`: ファイル上のカーソル位置
- `rx`: タブ展開後の画面上の x 座標
- `rowoff`: 画面上端に表示しているファイル行
- `coloff`: 画面左端に表示している列
- `screenrows`, `screencols`: 端末の表示サイズ
- `numrows`: ファイルの行数
- `row`: 行データの配列
- `dirty`: 未保存の変更があるか
- `filename`: 開いているファイル名
- `statusmsg`: 画面下部に出すメッセージ
- `syntax`: シンタックスハイライト設定
- `orig_termios`: Raw mode に入る前の端末設定

写経中に挙動がおかしくなったら、まず「どの状態が壊れているのか」を考えると追いやすいです。

## 行データ `erow`

ファイルの各行は `erow` で表されます。

```c
typedef struct erow {
  int idx;
  int size;
  int rsize;
  char *chars;
  char *render;
  unsigned char *hl;
  int hl_open_comment;
} erow;
```

重要なのは、`chars` と `render` が分かれていることです。

- `chars`: ファイルに保存される実際の文字列
- `render`: 画面表示用の文字列

タブ文字は、ファイル上では `\t` 1 文字です。しかし画面上では複数の空白として表示されます。そのため、カーソルのファイル上の位置 `cx` と、画面上の位置 `rx` は一致しないことがあります。

この違いがあるため、`editorRowCxToRx()` と `editorRowRxToCx()` で座標変換を行っています。

## Raw mode とキー入力

通常の端末では、入力は Enter を押すまでプログラムに渡されません。また、`Ctrl-C` などは端末側で特別扱いされます。

Kilo では、1 キーずつ入力を読みたいので、端末を Raw mode にします。

Raw mode の設定は `enableRawMode()`、元に戻す処理は `disableRawMode()` です。`atexit(disableRawMode)` により、プログラム終了時に端末設定を戻すようにしています。

キー入力は `editorReadKey()` で読み取ります。通常の文字はそのまま返しますが、矢印キーや Home/End/PageUp/PageDown は複数バイトのエスケープシーケンスとして届くため、独自のキーコードに変換しています。

## エスケープシーケンス

Kilo の画面描画では、ANSI エスケープシーケンスを使います。

よく出てくるものは次の通りです。

- `\x1b[2J`: 画面全体をクリア
- `\x1b[H`: カーソルを左上へ移動
- `\x1b[K`: カーソル位置から行末までをクリア
- `\x1b[?25l`: カーソルを非表示
- `\x1b[?25h`: カーソルを表示
- `\x1b[%d;%dH`: 指定した行・列へカーソルを移動

このあたりは 1 文字の違いで挙動が変わるので、写経ミスが起きやすい箇所です。

## 画面描画

画面描画の中心は `editorRefreshScreen()` です。

```text
editorRefreshScreen()
  -> editorScroll()
  -> カーソルを隠す
  -> カーソルを左上へ移動
  -> editorDrawRows()
  -> editorDrawStatusBar()
  -> editorDrawMessageBar()
  -> 本来のカーソル位置へ移動
  -> カーソルを表示
  -> まとめて write()
```

毎回画面を直接少しずつ書き換えるのではなく、`abuf` という append buffer に描画内容をためて、最後にまとめて `write()` しています。

これにより、画面のちらつきを減らし、描画処理も整理しやすくなっています。

## カーソルが左上に固定される問題

写経中に実際に詰まったのが、カーソルが常に左上に固定される問題です。

原因は、`editorRefreshScreen()` の中に、カーソルを左上に戻すエスケープシーケンスが余分に残っていたことでした。

```c
abAppend(&ab, "\x1b[H", 3);
```

画面を描き始める前にカーソルを左上へ移動するのは必要です。しかし、最後に本来のカーソル位置へ移動したあとで、もう一度 `\x1b[H` が実行されると、描画のたびにカーソルが左上へ戻ってしまいます。

この問題が厄介なのは、コードとしてはコンパイルできることです。コンパイルエラーではなく、実行時の見た目だけがおかしくなります。

また、チュートリアルの差分では、追加行に意識が向きやすく、削除された行を見落とすことがあります。特に、削除が大きく目立たない形で現れる step では注意が必要です。

対策としては、次のように確認するとよいです。

- `editorRefreshScreen()` 内でカーソル移動が何回行われているか見る
- 固定位置へ移動する `\x1b[H` と、動的位置へ移動する `\x1b[%d;%dH` を区別する
- 元記事の該当 step の完成コードと diff する

## `O` と `0` の見間違い

Home キーや End キーの判定では、次のようなコードが出てきます。

```c
} else if (seq[0] == 'O') {
```

ここは数字のゼロ `0` ではなく、大文字のオー `O` です。

フォントによっては見分けづらく、`seq[0] == '0'` と書いてしまうと、一部のキー入力が正しく判定されません。

このようなミスは、エラーとしては出ず、特定のキーだけ効かないという形で現れます。エスケープシーケンス周りは、見た目が似た文字に注意して、必要ならコピーではなく 1 文字ずつ意味を確認した方がよいです。

## 行編集の仕組み

Kilo の編集処理は、まず `erow` の中の文字列を操作し、そのあと表示用の `render` とハイライト情報を更新します。

主な関数は次の通りです。

- `editorInsertRow()`: 行を追加する
- `editorFreeRow()`: 行のメモリを解放する
- `editorDelRow()`: 行を削除する
- `editorRowInsertChar()`: 行内に 1 文字挿入する
- `editorRowAppendString()`: 行末に文字列を連結する
- `editorRowDelChar()`: 行内の 1 文字を削除する
- `editorUpdateRow()`: `chars` から `render` を作り直す

文字挿入や削除では `realloc()` と `memmove()` がよく出てきます。

`memmove()` は、コピー元とコピー先の領域が重なっていても使えるため、配列の途中に要素を挿入したり、途中の要素を削除して詰めたりする処理に向いています。

## ファイル I/O

ファイルを開く処理は `editorOpen()`、保存する処理は `editorSave()` です。

読み込みでは `getline()` で 1 行ずつ読み、末尾の改行文字を取り除いてから `editorInsertRow()` に渡します。

保存では、まず `editorRowsToString()` で全行を 1 つの文字列にまとめます。そのあと、`open()`、`ftruncate()`、`write()` を使ってファイルへ書き込みます。

保存が成功すると `E.dirty` を 0 に戻します。`dirty` は、未保存の変更があるかどうかを表すフラグです。終了時の確認やステータスバー表示に使われます。

## 検索機能

検索では `editorPrompt()` と `editorFindCallback()` が重要です。

`editorPrompt()` は、画面下部に入力欄を出し、ユーザーの入力を受け取ります。検索では、入力のたびにコールバックを呼び、現在の検索語に一致する箇所へカーソルを移動します。

`editorFindCallback()` では、以下のような状態を `static` 変数で保持しています。

- 前回一致した行
- 検索方向
- 検索前のハイライト情報

検索中に矢印キーを押すと、次または前の一致箇所へ移動します。Enter で確定、ESC でキャンセルです。キャンセル時には、検索前のカーソル位置とスクロール位置に戻します。

## シンタックスハイライト

シンタックスハイライトでは、行ごとに `hl` 配列を持ち、各文字がどの種類のハイライトかを記録します。

主な種類は次の通りです。

- 通常文字
- コメント
- 複数行コメント
- キーワード
- 型名などのキーワード
- 文字列
- 数値
- 検索結果

`editorUpdateSyntax()` は、1 行の `render` を見ながら、文字列、コメント、数値、キーワードを判定して `hl` を更新します。

複数行コメントでは、前の行からコメントが続いているかどうかを `hl_open_comment` で管理します。ある行のコメント状態が変わると、次の行のハイライトにも影響するため、必要に応じて次の行も再計算します。

## 説明とコード変更が前後して見える箇所

写経中に混乱しやすいのは、「コードの修正」と「その理由の説明」が常に同じタイミングで完結するとは限らないことです。

元記事は step-by-step で非常に丁寧に作られていますが、読み手側の状態としては、次のようなことが起こります。

- ある step でコードを追加したが、意味を完全に理解するのは少し後になる
- 一時的なテストコードがあとで削除される
- 以前の描画処理が、新しい描画処理に置き換わる
- エラーではないが、まだ期待した見た目にならない状態がある

このとき、「今の状態で正しいのか」を判断するには、元記事の compile status、該当 step の完成コード、自分のコードの差分を見るのが確実です。

## デバッグの進め方

写経中に詰まったときは、次の順番で確認すると切り分けやすいです。

1. `make` して、エラーか警告かを確認する
2. 直前に編集した関数を見る
3. 元記事の該当 step のコードと比較する
4. 画面描画の問題なら `editorRefreshScreen()` と `editorDrawRows()` を見る
5. キー入力の問題なら `editorReadKey()` を見る
6. カーソル移動の問題なら `E.cx`, `E.cy`, `E.rx`, `rowoff`, `coloff` を見る
7. ファイル内容の問題なら `erow`, `chars`, `render`, `numrows` を見る

特に、コンパイルは通るが動作がおかしい場合は、エスケープシーケンス、座標計算、削除し忘れた古い処理を疑うとよいです。

## 公開時の注意

このノートを GitHub で公開する場合は、元記事への敬意と出典を明記します。

- チュートリアル: [Build Your Own Text Editor](https://viewsourcecode.org/snaptoken/kilo/index.html)
- 著者: snaptoken 氏
- 元になったエディタ: [antirez's kilo](https://github.com/antirez/kilo)
- チュートリアルの Appendix: [Appendices](https://viewsourcecode.org/snaptoken/kilo/08.appendices.html)

元記事の文章やコードを長く転載するのではなく、自分の理解、手元で起きた問題、補足説明を中心にまとめます。

また、元記事ではチュートリアル本文と Kilo のソースコードでライセンスが分かれています。公開する場合は、元記事側のライセンス表記も確認し、必要に応じてリポジトリの README などにも出典を記載します。

## まとめ

`kilo.c` は 1 ファイルの小さなプログラムですが、C で実用的なソフトウェアを作るための要素が多く含まれています。

特に学びが多いのは、次の点です。

- 端末を Raw mode にして 1 キーずつ入力を読む
- ANSI エスケープシーケンスで画面を制御する
- 動的配列として行データを管理する
- ファイル上の文字列と画面表示用の文字列を分ける
- 小さな状態を積み重ねて検索やハイライトを実現する
- コンパイル警告や差分確認を使って写経ミスを見つける

写経では、コードをただ写すだけでも多くの発見があります。ただし、追加された行だけを追っていると、削除された行や 1 文字の違いを見落としやすいです。

うまく動かないときは、元記事の step ごとのコードと自分のコードを比べながら、どの状態が壊れているのかを小さく切り分けていくのが近道です。
